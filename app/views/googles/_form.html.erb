<%= form_for(@google) do |f| %>
  <% if @google.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@google.errors.count, "error") %> prohibited this google from being saved:</h2>

      <ul>
      <% @google.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.text_field :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
